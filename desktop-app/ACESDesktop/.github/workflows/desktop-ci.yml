name: Desktop CI pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [development]

jobs:
  java_tests:
    runs-on: ubuntu-latest

steps:
  - uses: actions/@checkoutv4
  name: 
  run: 
